// Global configuration for all API routes
// Force dynamic rendering for all API routes to prevent build-time static generation issues
export const dynamic = 'force-dynamic';
export const runtime = 'nodejs';
